export const MAX = 100;
export const INF = 9999;

export let pi: number[] = new Array(MAX).fill(INF);
export let p: number[] = new Array(MAX).fill(-1);
export let mark: boolean[] = new Array(MAX).fill(false);
